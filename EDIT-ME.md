# 先改这几个位置

这是给你自己继续填充的版本。全站署名占位符是 `YOUR NAME`，直接在 `index.html` 搜索替换即可。

优先替换：

- 顶部导航：`YOUR NAME / INDEX Nº101`
- Hero 小标题：`YOUR NAME — AI-native designer...`
- 页脚 `YOUR NAME`
- 作品目录中的 `title_cn`、`title_en`、`desc_cn`
- 作品目录中的 `path`（如果你添加了自己的真实项目页面）

视觉基调已经固定为：

- 粗野主义：硬边框、零圆角、编号、裸露网格、非对称留白
- 自然主义：石灰纸色、苔藓绿、泥土红、噪声纹理、手工感形状

如果想整体换色，只需要修改 `<style>` 顶部的 CSS 变量：`--paper`、`--card`、`--ink`、`--red`、`--dark`。
