# UPGRADE v2 — Atelier MIRA 总体规范（所有升级代理必读）

作品集定位：MIRA 的个人画室 —— 一间"追光的工作室"。美学根基两条：
- **自然主义**：对自然现象的直接观察——光、雾、水、风、季节、植物、地质、天象。每件作品研究一个真实现象，不是抽象装饰。
- **印象派方法**：外光派（plein air）用色、破碎笔触/分割主义色点、大气的瞬时印象、看得见的"绘制痕迹"（trace of the hand）。画面是"被画出来的"，不是"被渲染出来的"。

参考坐标：Monet 的干草垛与睡莲（同一对象的光色系列研究）、Pissarro 的田园、日本浮世绘对印象派的影响（平光、裁切构图）、植物学插画（Ellsworth / Redouté）、气象观测手稿。现代代码对标：Tyler Hobbs 的生成山水论文、Matt DesLauriers 的 canvas-sketch、Lusion / Active Theory 的层次感交互。

## 一、统一标识（每件作品必须全部出现）

1. **署名印章**：左上角内联 SVG 印章——外圈细环 + 手写感 "M" 字母（自绘 path，禁止用字体糊弄）+ 环上文字 "ATELIER MIRA"；旁边 "ÉTUDE N°xx/100 · 作品名"。印章主色取该作品 palette 的强调色。
2. **Colophon 页脚**：一行 mono 小字：媒介（Canvas 2D / WebGL / SVG / CSS）、当前 seed、"Painted with code, plein air"。
3. **种子系统**：URL hash 携带 seed（`#seed=xxxx`），刷新可复现同一画面；"再生"按钮换种子并更新 hash；有"复制分享链接"按钮（生成器类必须）。
4. **材质层**：画面叠加程序化纸纹/胶片颗粒/画布织纹（噪点 overlay，multiply/overlay 混合），拒绝平涂塑料感。
5. **动效语言**：慢、大气、物理感（风、水、光的运动学），避免弹跳 spring 的 UI 感；首屏即处于"正在发生"状态（绘制中/生长中/光在移动），零操作也有生命力。

## 二、交互深度（每件 ≥5 层，全部真实可用，禁止摆设）

1. **直接操作层**：拖/画/点，带物理反馈（惯性、流体、粒子响应、压感模拟）
2. **参数台**：≥3 个精修自定义控件（滑杆/旋钮/开关），带实时数值读数与单位
3. **构成模式层**：≥2 种构图/画法模式切换（同一现象的两种研究方式）
4. **氛围层**：时刻（晨/午/昏/夜）或天气（晴/雾/雨/雪）切换，全局光色联动——这是印象派的核心：同一母题的不同光色习作
5. **细节层**：hover 揭示（局部笔触放大 / 数值读数 / 标注线）、≥3 个键盘快捷键
6. **输出层**：下载 PNG/SVG（3D 类也要）；生成器类加"复制分享链接"
7. **彩蛋层**（加分项）：双击/长按/闲置 30s 触发的隐藏状态

## 三、对标纪律（动手前必做，写进回报）

用 WebSearch 找 1–2 个该作品类型里 GitHub 星数最高或 Awwwards 获奖的同类项目，回答"它强在哪 3 点？"，升级后逐项追上或差异化超越。回报里必须包含：对标项目名、差距清单、追平措施。

## 四、反面清单（AI slop，出现即返工）

- 蓝紫渐变 + 圆角卡片 + 居中大字
- 面板像 Bootstrap/Tailwind 默认组件；控件无自定义样式
- 滑杆只改一个 CSS 变量、视觉变化无层次
- 首屏静态、无氛围、无绘制过程感
- 纯色平涂几何、无笔触无颗粒无光影
- 文案 lorem / 空洞营销词（"极致体验""赋能"）
- 只有一层交互：点一下变一下，玩 10 秒就到底

## 五、技术红线（不变）

- 单文件自包含 index.html；零外链资源（无 CDN/字体/图片）；系统字体栈；无 emoji
- 仅 works/3d/ 可引用 `../../../vendor/three.min.js`（r128，BufferGeometry 系）
- rAF + devicePixelRatio + resize 自适应；移动端不破碎
- 完成后：内联 JS 抽出 `node --check`；如本机有 Chrome 则 headless 截图目检并迭代
- card.json 同步更新（desc_cn ≤40字有文案感、tags×3、palette 取升级后实际主色）
