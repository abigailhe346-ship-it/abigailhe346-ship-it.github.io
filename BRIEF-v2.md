# BRIEF v2 — 「印象自然主义 / Impressionist Naturalism」全面升级规范

> 所有升级代理**必读且逐条执行**。本文件替代 BRIEF.md 中与之冲突的所有条款（硬性规则一节除外，依然有效）。
> 用户原话："太烂了，每个项目都需要升级，带上独特的个人品味和标识，要让人眼前一亮而不是标准化的 AI slop。我偏好自然主义和印象派。每个项目的互动都要大幅度提升。"

---

## 0. 诊断结论：现在的作品为什么是 AI slop

抽查 10 件（截图实测，`--headless=new`，1440×900）后的结论。**最差的 3 件**：

1. **`3d/neon-wireframe`** —— 教科书级 slop：品红+青荧光、黑底、合成波条纹夕阳、发光霓虹字。这套配色就是"AI 生成网页"的全球通用脸。构图居中对称，除配色外无任何个人判断。
2. **`gen-art/aura-gradient`** —— 满屏浑浊的 teal 噪声团，无主体、无构成、无景深层次；控制面板是模板滑杆；色板是霓虹 spectra。像一张没调完的 shader 默认值截图。
3. **`agent-demos/prompt-lab`** —— 暗色 IDE 风开发工具模板：黄 CTA 大按钮、大面积空置的 completion 区、等宽字体堆砌。功能感有余，作品感为零，首次加载大半个屏幕是空的。

其余共性问题（**反例清单**，升级时逐条对照消灭）：

- **配色套路**：黑底 + 霓虹强调色（neon-wireframe、aura-gradient）；或安全但毫无性格的 SaaS 灰黑（eval-dashboard、prompt-lab）。
- **首屏即全部 / 或首屏没有全部**：black-hole 的编辑排版其实不错，但巨大的标题区把真正的黑洞渲染挤到一屏以下——访客第一眼看不到"哇"。aura-gradient 则相反，一屏看完，再无纵深。
- **控件模板味**：统一的"左侧滑杆面板 + 随机再生 + 下载 PNG"三件套，10 件里 8 件长一样。控件应该是作品世界的一部分，不是贴在画上的 DevTools。
- **动效无物理**：运动是线性推进或匀速旋转，没有惯性、没有阻尼、没有"呼吸"。
- **无叙事无时间**：画面是"某个瞬间的截图"，不是"一个正在发生的地方"。没有光的变化，没有天气，没有早晚。
- **扁平无质感**：矢量图形平涂（botanical-garden 的植物是剪贴画）、噪声平铺（aura-gradient），没有笔触、没有纸、没有空气。
- **执行尚可但方向错误**：rain-window 概念好，但"雨"渲染成了通用散景圆斑，玻璃上没有一条真的雨痕。flow-field 是抽查中最好的——纸色底、墨色流、有画意——**它就是升级方向的证据：这套系统能长出好东西，缺的是统一标准。**

### 与标杆的差距在哪（调研提炼）

- **Tyler Hobbs**（[Algorithmic Aesthetics](https://criticalplayground.org/cracks-in-the-code/)、[From Noise](https://solos.gallery/exhibitions/from-noise)）：flow field 的"手感"来自**刻意的失控**——他批判机器产物的过度光滑，让粒子按噪声场走短笔触、随机重置、密度随底图明暗变化（"translated gesture"：把画家的手势翻译成算法）。**启示：流场不是目的，笔触才是。**
- **Matt DesLauriers**（[color-wander](https://mattdesl.svbtle.com/generative-art-with-nodejs-and-canvas)）：粒子=沿速度方向的小线段；噪声尺度随机化让一些线紧卷、一些笔直；用照片亮度图驱动参数；**200 个人工策展的调色板**而不是随机取色；seeded RNG 保证可复现。**启示：色彩是策展出来的，不是随机出来的。**
- **Zach Lieberman**（[10 Years of Daily Sketches](https://nguyenwahed.com/exhibitions/33-10-years-of-daily-sketches-zach-lieberman-london/overview/)）：十年每日一练的"人味"来自**简单几何 + 随机性 + 身体性**（声音、手势、呼吸驱动画面）。"我像野生动物摄影师，等待我创造的那个野兽露出最好的瞬间。"**启示：作品要像一个活物，不是一个渲染结果。**
- **Casey Reas / Processing 传统**：软件即画布，每件作品是一个"过程"而非一张图。
- **GitHub 高星项目**：[paper-design/shaders](https://github.com/paper-design/shaders)（30+ 手工调参的 shader，每个都有策展过的默认 preset）、hydra（实时调制链）、three.js journey 式场景（物理相机、色调映射、舞台打光）。**共同点：受限但精准的参数空间 + 策展的默认值 + 物理感的运动。**

**为什么它们不像 AI slop**：不完美的手工感；受限但精准的色彩；运动有物理惯性；画面有"呼吸"；细节密度分层（远看构成、中看笔触、近看颗粒）；有概念叙事。下面的规范把这六点变成可执行条款。

---

## 1. 签名设计语言：印象自然主义 / Impressionist Naturalism

每件作品都是 Atelier MIRA 的一件"写生"：画的不是物体，是**某个时刻的光**。

### 1.1 色彩：七个具名调色板家族

每件作品**必须从下列家族中选一个**作为主基调（或按 1.1.3 规则自拟并命名）。hex 是锚点，允许 ±10% 明度抖动，但不允许跨家族混用超过 2 色。

#### ① 晨雾 Brume Matinale —— 冷，≈7500K
`#E8E6DF` 雾白 · `#C9CDD6` 雾紫灰 · `#9AA7B8` 远山蓝灰 · `#6E7B8C` 阴影蓝紫 · `#D8CBB8` 一线暖沙
> 适用：海景、山、雾、清晨城市。对比度全系列最低，最亮与最暗差值不超过 L*40。

#### ② 金色时刻 Heure Dorée —— 暖，≈3200K
`#F4E3C2` 蜜光 · `#E8B96F` 金 · `#D98E4A` 琥珀 · `#A85B3C` 焦赭 · `#5E6E7E` 阴影补色蓝
> 铁律：受光面走金→琥珀，**阴影必须是蓝紫系，禁止黑灰**。这就是莫奈不用黑色的原因（[参考](https://designforhackers.com/blog/impressionist-color-theory/)）。

#### ③ 睡莲 Nymphéas —— 冷中带暖斑
`#F2EFE6` 水光 · `#DCE4DA` 浅漪 · `#A9BFAD` 柳绿 · `#5F7D6E` 深塘 · `#7C8FA6` 天光倒影 · `#C9A6B4` 莲粉（点缀 ≤8% 面积）

#### ④ 罂粟田 Champ de Coquelicots —— 阿让特伊的夏午
`#B7C98B` 草甸 · `#7A9A5B` 深草 · `#8FA3B8` 远天 · `#E4D9C2` 小径 · `#6B5D4F` 泥土 · `#B43A2E` 罂粟红（**唯一的纯色，面积 ≤10%，只能点状出现**）

#### ⑤ 薰衣草 Lavande —— 黄昏前一小时
`#E8E0D0` 石灰墙 · `#D9C7E0` 远紫 · `#B9A8C9` 花田 · `#8E7BA8` 中紫 · `#6B5B85` 深紫 · `#4E4A6B` 暮影
> 紫色只允许出现在这个家族，且必须是灰调紫。**禁止任何荧光紫。**

#### ⑥ 潮汐 Marée —— 诺曼底海岸
`#E3E0D5` 贝壳白 · `#B5C2C4` 浅滩 · `#7E9399` 海水 · `#4F666E` 深潮 · `#C8B89A` 湿沙 · `#D97B5A` 浮标锈红（点缀）

#### ⑦ 林间光 Sous-bois —— 穿过叶隙的光
`#E8E3CE` 光斑 · `#B5B98A` 亮叶 · `#7E8F5E` 中绿 · `#4E5E42` 深叶 · `#2F3B2C` 林翳（全系列最暗，到此为止，禁止更暗）· `#D9A85C` 金色斑光

#### 1.1.1 禁止清单（出现即打回）
- 霓虹蓝紫渐变（`#6366F1→#A855F7` 一族）、青粉荧光、品红发光字、synthwave 条纹夕阳
- 纯黑 `#000` 与纯灰阴影（阴影必须带色温：暖光的阴影偏蓝紫，冷光的阴影偏赭）
- 大面积（>30% 画面）无调制平涂色块
- Tailwind 默认 indigo/slate 直出、"深色底+一个荧光强调色"的 SaaS 配方

#### 1.1.2 色温规则
每个家族标注了色温。画面内所有颜色必须服从该色温：暖家族里出现冷色，只能是点缀且降饱和；冷家族反之。自检方法：把截图在脑海里转成黑白，再问自己"这是几点钟的光"——答不上来就是失败。

#### 1.1.3 自拟家族
允许，但必须有名字（中/法双语）、6 个 hex、一句色温说明，写进代码顶部注释。名字要像一个地方的一个时刻，不要叫"Tech Blue"。

### 1.2 质感：把"画"画出来，不是"填"出来

Canvas/WebGL 实现要点，**每件至少用 3 条**：

1. **碎色笔触（broken color）**：任何面积 >200px² 的色域禁止 `fillRect` 平涂。拆成 200–2000 根短笔触（长 3–12px），每根颜色 = 基色 + 色相抖动 ±8° + 明度抖动 ±10%，alpha 0.5–0.85，笔触方向跟随局部流向（flow field / 法线 / 重力）。
2. **点彩混色**：需要混两个颜色时，不用 `mix(c1,c2)` 算中间色，而是以 60–80% 密度交替点画两色的 2–4px 圆点，让眼睛自己混（divisionism）。
3. **大气透视**：远景层向"地平线色"收敛——`final = mix(color, fogColor, pow(depth, 1.5) * 0.8)`，同时降饱和。画面必须有**前、中、远三层**可辨认的景深。
4. **纸纹/画布纹**：离屏 canvas 预生成一次（4000+ 个 alpha 0.02–0.06 的斑点 + 少量长纤维线），以 `multiply` 或 `overlay` 混合模式、3–8% 不透明度叠加全画面。**禁止**用 CSS 渐变假噪点。
5. **颗粒**：最终合成前加 1–3% 单色胶片颗粒，以 12fps 更新（60fps 的颗粒显得神经质）。
6. **笔触生命**：笔触可以"生长"——落笔时细短，随粒子运动拉长变淡，像颜料被拖开。

### 1.3 光：每件作品必须有时间感（一致性签名）

这是贯穿 100 件的**第一签名**。

- 每件作品定义变量 `heure`（0–24），默认取访客本地时间 `new Date().getHours()`。
- 光的色温、方向、强度随 `heure` 插值：晨（冷平光）→ 午（白顶光）→ 暮（长影金光）→ 夜（蓝紫+点光源）。色温插值用 HSL/OKLCH，不用 RGB 直插。
- 必须提供一个"时间"交互：拖动时间轴 / 滚动推进 / 长按加速日落。**访客必须能在 10 秒内看到同一场景的两种光。**
- 3D 作品：太阳位置 + 环境光色 + 雾色三者联动；Canvas 作品：全局叠加层色温 + 影长 + 高光色联动。
- 夜不是黑：夜的底色至少是 `#2F3B2C` 级别的"有色的暗"。

### 1.4 标识系统（贯穿 100 件）

- **Atelier 名**：`Atelier MIRA`（与总站同名）。落款固定格式：
  `Atelier MIRA · N°<全局编号 001–100> · <调色板家族名>`，位置左下或右下，10–11px 等宽/小号衬线，字距 +0.12em，不透明度 60–75%。
- **Monogram**：手绘感 "M" 与燕子剪影组合的小标（16–24px），置于落款左侧。燕子用下面 §1.4.1 的母题 SVG 缩小版。
- **编号格式**：全局编号 `N°001–N°100`（升级代理从总站策展顺序取号；类别内序号保留为副标，如 `Gen-Art · N°15`）。
- **题记**：每件作品标题下**一句**法文或中文题记，斜体衬线，14–16px。法文用书名号 « … »，如 « Le vent d'avril traverse les blés. »；中文一句手记，如「四月的风穿过麦田，颜色比麦子先到。」**禁止写成技术说明**（见 §1.5）。

#### 1.4.1 母题：一只燕子 L'Hirondelle

一只水墨剪影燕子贯穿全部 100 件：落款里常驻小剪影；在部分作品里作为活物飞过（彩蛋，见交互 L4）。标准 SVG（内联使用，深色版本 `fill="#2b2b26"`，浅底场景可改为家族最深色）：

```html
<svg width="22" height="14" viewBox="0 0 64 40" fill="#2b2b26" aria-hidden="true">
  <path d="M32 20 C 26 12, 14 6, 4 8 C 12 12, 18 18, 24 21
           C 14 20, 6 22, 2 28 C 10 26, 18 26, 25 26
           C 27 30, 30 34, 34 36 C 33 32, 33 28, 34 25
           C 42 22, 52 16, 62 4 C 54 10, 44 16, 36 19
           C 35 17, 33 17, 32 20 Z"/>
</svg>
```

> 第一位升级的代理须手工微调这条 path 到"一眼是燕子"（分叉尾、后掠翼、头朝右），然后把定稿 path 原样复制给所有作品——**100 件必须共用同一条 path**，这是识别度的来源。

### 1.5 排版与声音（tone of voice）

- **字阶**：作品名 = 展示衬线（`Didot, 'Bodoni MT', Georgia, 'Songti SC', serif`，可 italic 混排中西文）；题记 = 斜体衬线；数据/参数标签 = 小号等宽（`SF Mono, Menlo, monospace`）全大写 + 宽字距。禁止无衬线大字标题。
- **声音**：所有界面文案是"画室手记"，不是"产品说明"。
  - ✗ 「这是一个粒子流场模拟器，调节参数查看效果。」
  - ✓ 「千支笔顺流而下。搅动它，就改了河道。」
- **10 条例句**（仿写，不要照抄）：
  1. 四月的风穿过麦田，颜色比麦子先到。
  2. 雨停之后，玻璃窗还留着半个城市。
  3. 光线在下午四点开始变得值得原谅。
  4. 潮水退去的地方，沙子记得每一道波纹。
  5. 这座花园只在熄灯之后营业。
  6. 雾不是白色的，是所有颜色都后退了一步。
  7. 把声音关小一点，就能听见笔触。
  8. 一只燕子迟到了，整个春天都在等它。
  9. 影子不是黑色的，是蓝色的地方在休息。
  10. 我们画的不是睡莲，是水面上的下午。
- 技术参数（RS 1.00、seed、步数）允许出现，但只准出现在等宽小字里，是"画框背面的铅笔字"，不是卖点。

---

## 2. 交互深度标准

交互分五层。**每件作品至少覆盖 3 层，且必须有 1 个 wow moment**（定义：访客会想要录屏发给别人的那一瞬）。层级越高越差异化。

### L1 · 即时物理反馈（必选项）
1. 指针跟随用阻尼插值（`pos += (target - pos) * 0.06~0.12`），永不直接赋值。
2. 拖动有惯性：松手后速度按 0.92–0.96/帧衰减。
3. 笔触/粒子方向跟随指针**速度向量**，不是位置——快速划过激溅，慢速晕染。
4. hover 产生涟漪/扰动场，影响半径随停留时间扩大。
5. 点击=压力波：以点击点为源的环形位移波，振幅指数衰减。
6. 所有 CSS 动效只用 `cubic-bezier(0.22,1,0.36,1)` 或弹簧物理，禁止 `linear`/`ease` 默认值。

### L2 · 参数即乐器
1. 每个滑杆实时作用于渲染，**零重启、零重绘闪烁**；拖动过程本身就是表演。
2. 参数预设做成"练习曲 Études"：3–5 个策展过的具名预设（如「晨」「骤雨」「将夜」），一键切换整组状态。
3. 键盘可演奏：A–L 键映射参数/触发，按住有持续效果。
4. 参数状态写入 URL hash（`#h=18&m=pluie`），可分享复现。
5. 滑杆做成作品世界的一部分：画在画布上的刻度、水位线、光线角度尺，不是 HTML 默认 range。

### L3 · 时间与天气（签名层，强烈建议）
1. `heure` 时间系统（§1.3）+ 可拖动的时间轴。
2. 天气模式 ≥2 种（晴/雾/雨/风），天气改变粒子行为与声景，不只是滤镜。
3. 滚动=时间：向下滚动推进一天，从晨到暮（scroll choreography）。
4. 季节变量：同一构图四套 palette（在 §1.1 家族间映射）。
5. 日月星辰位置遵循真实时间（粗略即可，但要自洽）。

### L4 · 叙事与彩蛋
1. **燕子飞渡**：每 5 件作品中至少 1 件，燕子（§1.4.1）在随机时刻横穿画面一次，翼尖扰动粒子。
2. 静置 20 秒进入"呼吸模式"：镜头极缓慢推移，画面自主演化，像一幅活的画。
3. 隐藏题记：连续点击落款 3 次，浮现一段手写体手记。
4. 节气彩蛋：访问当天若逢节气（内置 24 节气日期表），画面出现对应变化（谷雨落雨、霜降结霜）。
5. 作品间互文：A 作品里飞走的鸟，是 B 作品里落下的羽毛——至少在题记文案层面互相指认。

### L5 · 生成音频环境声
全部用 WebAudio 实时合成，**禁止音频文件**（自包含规则）。
1. **风**：brown noise → bandpass（400–800Hz，Q≈1），截止频率随风力参数缓慢摆动。
2. **雨**：white noise → highpass 1.2kHz + 随机短促水滴（sine 1800→600Hz 下滑，40ms，指数衰减）。
3. **鸟鸣**：FM chirp——载波 2400→3200Hz、80ms、调制指数随机，间隔 2–9 秒泊松分布，音量 -18dB 以下，是远景不是闹钟。
4. **笔触沙沙**：filtered noise 以指针速度为门限，速度越快越响，停笔即止。
5. **钢琴音簇**：五声音阶（宫调或 dorian）随机挑 2–3 音成簇，`sine + 轻微 detune`，3–6 秒自然衰减，由特定交互触发（如金色时刻到达）。
6. 混音规范：所有声源进同一个 `GainNode`（master），默认 **OFF**；右上角一个 SVG 小鸟/声波开关，点击后 1.5 秒淡入；状态存 `localStorage`；**绝不自动播放**。

---

## 3. 反 slop 检查单（交付前逐条勾选）

- [ ] 01 无霓虹蓝紫渐变、无青粉荧光、无 synthwave 夕阳
- [ ] 02 无纯黑 `#000` 底；最暗色 ≥ 家族最深色；阴影带色温（补色方向）
- [ ] 03 主色明确属于 §1.1 某个具名家族（或按 1.1.3 自拟并命名）
- [ ] 04 点缀高饱和色 ≤10% 面积，且全画面只有一个
- [ ] 05 构图非对称：主体偏离画面中心，有负空间呼吸
- [ ] 06 有可辨认的前/中/远三层景深，远景经大气雾化
- [ ] 07 大色域全部用碎色笔触/点彩，无 >200px² 平涂
- [ ] 08 有纸纹或颗粒等有机质感层
- [ ] 09 所有运动有阻尼/惯性/缓动，无 linear 匀速运动
- [ ] 10 首屏 3 秒内呈现完整且正在运动的画面（不接受"先看到大标题，作品在第二屏"）
- [ ] 11 控件全部自定义样式，融入作品世界，无浏览器默认外观
- [ ] 12 控制面板可一键收起（H 键 + 明显入口），展开时不超画面 20%
- [ ] 13 有时间感：`heure` 存在，且访客 10 秒内能见到两种光
- [ ] 14 覆盖交互层 ≥3 层，且能说清 wow moment 是哪一帧
- [ ] 15 有音频则遵守 L5 规范（默认 OFF、一键开启、淡入、可记忆）
- [ ] 16 题记是一句手记/法文引句，不是技术说明
- [ ] 17 落款完整：燕子 monogram + `Atelier MIRA · N°xxx · 家族名`
- [ ] 18 燕子母题出现（落款常驻；或 L4 彩蛋）
- [ ] 19 无 emoji 图标；图标全部 SVG/文字
- [ ] 20 无任何外链资源（全文搜索 `http://`、`https://`、`url(`，仅允许注释与文案）
- [ ] 21 375×812 移动端截屏不破碎（canvas 自适应、控件不溢出）
- [ ] 22 文案无 lorem、无"这是一个 XX 模拟器"句式

---

## 4. 强制 QA 闭环（不走完不算完成）

1. **截图命令**（本机 Chrome，逐字使用）：

```bash
"/Applications/Google Chrome.app/Contents/MacOS/Google Chrome" \
  --headless=new --hide-scrollbars \
  --window-size=1440,900 --virtual-time-budget=6000 \
  --screenshot="/tmp/qa-<slug>-r1.png" \
  "file:///Users/moonshot/ai-portfolio/works/<category>/<slug>/index.html"
```

   ⚠️ **必须用 `--headless=new`**。旧的 `--headless --disable-gpu` 会把 WebGL/three.js 渲染成纯黑画布，已实测造成误判。移动端把 `--window-size` 换成 `375,812`。
2. **亲眼看**：用 ReadMediaFile 打开截图，写下至少 3 条具体问题（对照 §3 检查单），改完再截。**至少 2 轮**（`/tmp/qa-<slug>-r1.png`、`-r2.png` 都要真实存在）。
3. **JS 语法**：把内联脚本抽出 `node --check`，或在 Chrome headless 日志中确认无 console error。
4. **外链自查**：`grep -nE 'https?://' index.html`，只允许注释与文案。
5. **验收清单**（回报时必须逐项确认）：
   - [ ] r1/r2 两张桌面截屏 + 1 张移动端截屏，均亲眼检查过
   - [ ] §3 检查单 22 条全过
   - [ ] 交互层数与 wow moment 写明
   - [ ] 调色板家族名写明
   - [ ] `card.json` 更新（palette 三个 hex 取自新画面主色，desc_cn 用手记体）

---

## 5. 保留的硬规则（与 BRIEF.md 一致，继续有效）

1. 每件作品 = `works/<category>/<slug>/index.html`，**单文件完全自包含**（CSS/JS 内联），禁止任何外部网络资源；字体只用系统字体栈。
2. 唯一例外：3D 类可引用 `<script src="../../../vendor/three.min.js"></script>`（r128，全局 `THREE`）。其余一律零依赖。
3. `card.json` 格式不变：`{"slug","category","title_en","title_cn","desc_cn"(≤40字,手记体),"tags"[3],"palette":[3 个 hex]}`。
4. requestAnimationFrame + devicePixelRatio + resize 自适应；移动端不破碎。
5. 不要 emoji 图标；无 lorem ipsum。

---

## 6. 升级建议路径（供编排者参考）

- 先试升级 1 件（建议 `gen-art/flow-field`，底子最好），跑通 §4 全流程，作为"标准件"供其余代理对照。
- `3d/neon-wireframe`、`gen-art/aura-gradient`、`agent-demos/prompt-lab` 无升级价值，建议按本规范**重做**。
- 全部完成后，总站 MIRA 的策展页也需套用同一落款与调色板规范（另行发简）。
